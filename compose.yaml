services:
  api:
    container_name: turbo-server
    image: khizer30/turbo-server:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - apps/api/.env.production
    restart: unless-stopped
